# 数据库 ORM：用 Python 代码操作 MySQL
sqlalchemy>=2.0,<2.1
# MySQL 驱动（纯 Python 实现，无需编译）
pymysql>=1.1
# MySQL 8 默认认证方式需要这个加密库
cryptography>=42.0
# Redis 客户端（app/cache.py 使用）
redis>=8.0,<9
# 读取 .env 配置文件
python-dotenv>=1.0

# 向量库与 RAG（langchain 全家桶）
langchain-milvus>=0.4
langchain-openai>=1.1
langchain-text-splitters>=1.1
# 注意：langchain-community 用独立 0.x 版本号（当前最新 0.4.x），
# 与 langchain 核心的 1.x 版本号不同步，不要写成 >=1.0
langchain-community>=0.4,<0.5
dashscope>=1.20

# Web 框架
fastapi>=0.135
uvicorn>=0.46

# 测试
pytest>=8.0
httpx>=0.27
